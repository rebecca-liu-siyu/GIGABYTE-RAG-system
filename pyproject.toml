[project]
name = "rag-project"
version = "0.1.0"
description = "RAG assistant for GIGABYTE AORUS MASTER 16 AM6H"
readme = "README.md"
requires-python = ">=3.11,<3.12"

dependencies = [
    "beautifulsoup4",
    "requests",
    "numpy",
    "pandas",
    "faiss-cpu",
    "sentence-transformers"
]